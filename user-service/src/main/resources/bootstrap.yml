server:
    port: 8010
    servlet:
        context-path: /api
spring:
    application:
        name: user-service
    data:
        mongodb:
            host: localhost
            port: 27017
            database: creative-drive-latam
    cloud:
        config:
            uri: http://localhost:8088